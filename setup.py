from setuptools import setup

setup(name='hessidf', version='0.0', description='Load the HESSI data into a pandas dataframe and some other funtions for filtering and plotting',
      author='Matt Wingham', author_email='matt.wingham97@gmail.com', packages=['hessidf'], install_requires=['numpy', 'matplotlib, 'pandas'])